flora
multi
pqq

======================================
In-Progress / Needs human verification
======================================
- Pricing

===============
Pending Prompts
===============
- tests on pricing
  - On 3+ night stays, the guest fee is 0%. ?
  - % over weekday UI needs changed to match Airbnb. I.E don't use radio groups
  - No point showing price on yellow cel (unbookable)
  - Increase font size of money in cell?
  - Copy / guidance / copy needs improved overall
  - Needs instruction on calendar page to le tthem know about long press?
- Cleaning fees
- Extra guest fee


=============
MVP: Host app
=============
--------------
High prescence
--------------
- Menu -> Earnings
- Menu -> Payments
- How to view images on desktop, should open in lightbox? MVP
- Highland Hosts setup
  - Requires: F&H or Sando IT
- Cancellation policy - full policy modal
- "self check-in with lockbox" field
- Beds/bedroom granularity like airbnb (Photo tour) - What is solution here?
- Co - host test invite and listing access [Blocked awaiting dnb thing]
  - Resend: Deep link solve.
    - Publish app to google play (Beta)
    - docs/user-todos/universal-links-setup.md
      - ## Android (App Links)
      - ## App Store URLs
      - ## Native Rebuild Required
- App: Edit listing - Bookings tab - Instant book
- (Arrival guide remaining components)
- Listing Creation decision - Should it include minimum set of data to list or not. Host guidance
  - Draft or Publish after creation?
- Pricing
- Booking events
  - Requested & confirmed
  - Instant
  - Cancellation
- Scheduled messages
  - Should they show in conversation thread
  - Should host received a notification for outbound scheduled messages (like Air BnB)
- Payments (Stripe or Plaid)
- SKYE_ENVIRONMENT doesn't make much sense in context of app. as it has local, preview, production etc expo dists...
  - It's used by sentry, its passed it in as sentry env... also look at eas.json.
- Queue policy needs review (retry speed etc)
- Security: Confirm peoples identity or address, otherwise anyone could hots a prop and collect money?
  - Decide address upload or stripe verification (account vs name match)

-------------
Low prescence
-------------
- Message template add details..comment out non implemented?

==================
MVP: Guest website
==================
---------------
High prescence
--------------
- Show booking price in preferred currency>?
- Instant Booking flow / as it pertains to opening confirm and pay modal and login/register
- Request to book Message hots button on listing detail page?
- Show more modal on listing detail page, scrollbar shows on mobile when not needed
- Decide & implement if public booking possible via signup during booking flow.
- Cancel booking flow

--------------
Low prescence
--------------

===============
Post-MVP
===============
- iCal sync between skye-glamping and highland-hosts for Croft8
- Test face auth (IOS) biometrics, as it uses face not fingerprint apparently.
- docs/user-todos/universal-links-setup.md
  - ## Apple (iOS Universal Links)
- Improve twilio email verification spam folder avoidance
  e.g configure SPF, DKIM, DMARC
- How can I test host app from my device if not dev API, does it matter?
- Enable tracing for sentry? - Can exceed free tier though...
- Stripe secret and publishable key on prod are using sandbox creds, need replaced
- Review const protectedPaths = ['/dashboard', '/bookings', '/account']; in each app
- Solve env vars in apps/f/e better as vercel doesn't support org level env vars
  and dont want repat across projects. but want to keep simple and no anti pattern.
- Next Steps for You (Cohost)
  - The invite link deep link (skye-hosts://co-host/invite-landing?token=xxx) works with the existing scheme: "skye-hosts" config. For production, you'll want
  universal links (HTTPS) too so the link works when the app isn't installed.
  - Email sending is stubbed — the owner shares the invite link manually (Share sheet).
  - The sign-up-via-invite flow collects phone + OTP + name, but doesn't yet set the invitee's email on their account. You'll need a PATCH /account endpoint or add
  an email param to phoneVerifyOtp to link the invite email to the new account. Without this, the acceptInvite email check will fail for phone-only signups. Want me
  to add that?

==============
Untested list:
==============
- test long listing name
- iCal export
- iCal booking.com
- Calendar sync health indicator (filled circle), all scenarios

==========================
Notes about copying AirBNB
==========================
You cannot copy:
  Visual layouts that are highly distinctive
  Icons, illustrations, photos
  Exact wording
  Screen designs that are “substantially similar”
  Function is OK. Expression is protected.
You can't market as "AirBNB but cheaper"
TLDR:
  - Don't use exact wording
  - Diverge layout/style slightly where possible

On both thes pages in host app:
  - Create message template
  - Edit message template.

























- Currently setting pricing for a Hosts Air BnB listing is unimplemented, let's implement it
- Help me refine the feature and then propose a plan to implement.
- Any major decisions ask, scrutinise my idea for bad assunmptions, decisions etc.
- Identify superior approaches where possible
- Re-use existing components/stylers where possible, if new styles required, ensure oy make them re-useable
- Business Goals:
  - Primary incentive for hosts of using Skye Hosts is lower fees than Air BnB.
  - Most of this benefit should be passed to Host, (10% ish savings), with Guest recieving 2% benefit approx.
  - Air BNB charges guests about 21% on average for a 2 night booking, and hosts 3%, for a 3+ night booking their guest fee drops to 15%
  - Skye hosts will aim to charge hosts 3%, and guests about 3% + 3% payment processing (stripe), so 6% in total. This is about 12% cheaper than air bnb on a 2night booking and 9% cheaper on 3 night booking.
  - Hosts use Air BNB as their primary booking system, therefore
  - We want it to be very simple (low cognitive load) for hosts to input a price for listing here that has price parity with (AirBNB - 3% approx) (from guests perspective)
    - I.E THey can look at their Air BNB actual net income receievd (after all feees deducted) and use that as a basis for what they charge in skye hosts)
    - I.E Guests will be incentised to book with Skye hosts because it's about 3% cheaper
    - Hosts will be incentivised because they can save about 10%.
  - We want it to be clearly displayed to host:
    - How much they can expect to save on fees
    - How much they will earn, net per night after subtraction of guest fee, host fee, cleaning fees, extra guest fee etc.
  - Provide guidance to increase clarity where possible to the host using existig
- Don't worry about iuncluding this in the listing creation flow, it can just be done after creation.. Ensure it is easily re-usable though if we want to include later whilst keeping things DRY.
- Ensure all code for calculating prices etc is re-useable.
- Document this all in /plans/architecture so we can pick it up later. (Once plan finalised)
- Approach:
  - Page 1: On 'Listing editor' page in hots apps, inside the 'Booking' tab, in the 'Pricing' card, display:
      - 'Enter details' (Copy how other cards do it) if no pricing data set, otherwise:
      - Show quick summary of pricing data if already set (Up to 3 lines max)
    - After taping on the pricing card, on the ezxisting 'Pricing page', add capability to set pricing for their listing.
    - There will be no smart (dynamic pricing), no promotions (implemented later)
    - On the pricing page, add sections:
      - Section 1: Seasons:
        - Title 'Seasons';
        - List items:
          - List item 1:
            - Title: Low Season (Winter)
            - Subtext: November → February
          - List item 2:
            - Title: Shoulder Season (Spring & Autumn) 
            - Subtext: March → April and September → October
          - List item 3:
            - Title: Peak Season (Summer)
            - Subtext: Remaining months
          - Re-use existing list item components/styles. 
          - Displsay 'Edit' on right hand side
          - Each list item has Extra subtext (only if value set)
            - Display Summary of: 'Weekday price, weeeknd price, discounts available (For discounts only show 1, if more, show + n other discounts)
        - Display each season in a card (vertically stacked)
        - When a season is tapped on, open a modal which starts a wizard with approx 3 srtages:
          - Stage 1: Set a weekday base price
          - Stage 2: Set a weekend base price
            - This can be specified via either an absolute number or via a percentage over weekday bas price
              - If percentage slider is changed, dynamically up date the absolute value shown on-the-fly
              - Set percentage to default of 10%
          - NB in both weekday and weekend price pages.
            - Below price input, show:
              'Guest price £XXXX' with a down ehvron, that when tapped. Expans something below:
                - Thw shows a price rebakdown resulting in guest fdee
            - Also show, 'Learn more about pricing' link which when tapped, opens a modal explaining how pricing works
            - These two stages can be very similiarily layoued out /styled
            - Content should be centered, including title, text, to make intuitive for user. S
          - Stage 3: Discounts, possibleoptions include:
            - Last-minute Discount
              - Can be toggled on/off
              - Set %, default 5%
              - Description: "For stay sbooked 14 days or less before arrival"
            - Weekly discount
              - Can be toggled on/off
              - Set %, default 10%
              - Description: "For stays of 7 nights or more"
            - Monthly discount
              - Can be toggled on/off
              - Set %, default 20%
              - Description: "For stays of 28 nights or more"
          - Wizard should include next, cancel, back buttons etc at bottom.
          - NB, there is WizardAppBar existing which may be of some use.
      - Section 2. Cleaning fee (not seasonal)
        - Display title 'Cleaning fee'
        - Card:
          - Title: 'Per stay'
          - Text: '£1' (Large font-size) (Current value) or if no current value, display 'Enter details'
          - Don't implement front-end code for host to set value here (it will be done later), just assume for now it's £1, and setup required backend code
          - We include backend code now, as this may affect pricing shown to guests. 
      - Section 3. Extra-guest fee
        - Display as a card, when tapped, opens form-input-modal
        - "After N guests, per night":
        - Excludes babies, only includes adults, children, etc
        - Don't implement front-end code for host to set value here (it will be done later), just assume for now it's possible that it will exist in future. (Should be incproprated/included into any price calcs if set). Setup required backend code for it.
      - Include the following helpful guidance for clarity
        - The price you set is how much you will receive after all fees deducted (I.E after cleaning fees, skye hosts fee etc) how much it will arrive in your bank account
        - Important: 
          - Recommend that they first figure out what their AirBNB net nightly income is for this particular season and then set a value here that is about 10% more. E.G They will earn 10% more if a booking happens here. And the guests will pay about 2% less as well
          -
  - Page 2: Listing calender page:
    - We want it to be simple and fast to set pricing on a seasonal basis. E.G peak, off-peak, shoulder. Then for granular adjustments on specific dates they can override these baselines.
    - The overriding capability should be handled on the listing calendar page by clicking on a day cell (multiple) using existing thumb drag select
      - In the existing slider that cxomes up from bottom (that includes the button 'Block these dates'), add an additional button that approx text "Set price override/manual" (You decide best wording)
        - This would then open a modal to set overrides. 
        - This modal would also display:
          - How much it's advertised to Guest (E.G price includin host fee + guest fee + cleaning fee + extra guest fee etc.)
    - The unbooked, unblobkced, available day cells in present -> future (not past) should be updated to show:
      - Price advertised for. (Where £ represents net income for Host) (grey text)


  -  1. 
    Incentivising hosts is the priority.
    Guest incentive is secondary. (I.E If price as same as AirBnB not a huge deal, but ideal is a small reduction)
    The 12% I provided was on a 2 night booking whereby AirBnB takes 18% guest fee).
    Update our proposal to copy this pattern. E.G For 1-2 night bookings = % and 3+ night bookings = % - 3% (For guest )
    Double check our asumption about airbnb gurest fee is accurate. Check we have approximate parity.
    On both 1-2 night bookings and 3_+ night bookings we want to create about a 12% gap
    Don't word it like 'Up to % cheaper', word it like '% cheaper'
  - 2. Agree
  - 3. Agree. The advertised price can exist only when overriding prices on a day cell it show in resulting mnodal
  - 4. We will ship the 'cleaning fee' feature before our product is live, the £1 is purely to ensure it is correctly incorporated into any pricing cacls, for testing purposes, it will default to zero once live. But do as you see best.
  - 5. Agree
  - 6. Agree
  - 7. Agree
  - Decision: a-f: Proceed with your recommendation/default
  - Decision g: What are pros cons of a separate 'pricing' package, as opposed to next best alternatei which I'm assuming is 'packages/skye-hosts-api-client'
  - Decisions H: Yes build endpoint but no frontend for guest website