====
Next
====
- Co - host test invite and listing access [Blocked awaiting dnb thing]
  - Resend: Deep link solve.
    - Publish app to google play (Beta)
    - docs/user-todos/universal-links-setup.md
      - ## Android (App Links)
      - ## App Store URLs
      - ## Native Rebuild Required
  - Get Expo pipeline (GH) deployed to real device

===
MVP
===
- Add 'Local laws' AirBnB equivlant to listing creator
- App: Edit listing
  - (Your space, remaining components)
    - Instant book
    - Cancellation policy
  - (Arrival guide remaining components)
  - Understanding why some data not colecting during creation, only edit... -> Plan
  - Draft or Publish after creation?
  - Availability
  - Pricing
- App: Calendar
- App: Co-host
  - Send Co-hist invite via twilio sms
- Booking events
  - Requested & confirmed
  - Instant
  - Cancellation
- Scheduled messages: Understanding, if should show in conversation thread
- Payments
- SKYE_ENVIRONMENT doesn't make much sense in context of app. as it has local, preview, production etc expo dists...

=======
General
=======
- flex-grow on <main not working>
- Shift postman to skyehosts email
- Shift stripe to skyehosts email & update secrets
- expo build working on real device?
- env plan: Pipeline green, deployed agian
  - ignore test-results
- Update pre-push to on qa branch only do aws-infra gate
- Update deploy-prod, to only deploy cdk if there's been a change
- add sentry dsn to expo vars for deployed app?

=============
Admin website
=============

==================
Aws infrastructure
==================
- Queue
  - Queue policy needs review (retry speed etc)
  - DLQ tested as working

========
Host App
========
- If you delete all rows from Account, you appear to still be ble to login into app xD
- name not required on signup again?!
- docs/user-todos/twilio-email-verify-setup
- docs/user-todos/unosend-email-setup.md
- Listing title, vertical height is too long
- Test
  - Notification
- Login / Sign - up separation in app.?
- deploy to device succesfully via eas / gha
- Test scheduled messages arriving after booking
- Notifications (Messages from guest)
- SMS sending OTP (Twilio or other ?)
- Today page
- Calendar page
- Menu page
  - Change password
  - Change email
- If you already signed up and reinstalled app, could you gain access

=============
Guest website
=============
- Check that sentry error 'def' capture dby sentry (isr)

===============
Long term TODOs
===============
- Do need to put validation on images with regards to weird ratios or, put a cropper in 
- database.service.ts is anti pattern I think not necessary
  - This is because of getRepositorty() being almost pointless and in modules it's meant to export TypeOrm.ForFeature()
  - Also double check but I think perhaps the transaction approach in thi service has fragility issues
- docs/user-todos/universal-links-setup.md
  - ## Apple (iOS Universal Links)
- Improve twilio email verification spam folder avoidance
  e.g configure SPF, DKIM, DMARC
- email verification (using same twilio sms service)
- How can I test host app from my device if not dev API, does it matter?
- Enable tracing for sentry? - Can exceed free tier though...
- Stripe secret and publishable key on prod are using sandbox creds, need replaced
- Review const protectedPaths = ['/dashboard', '/bookings', '/account']; in each app
- Solve env vars in apps/f/e better as vercel doesn't support org level env vars
  and dont want repat across projects. but want to keep simple and no anti pattern.
Next Steps for You (Cohost)
  - The invite link deep link (skye-hosts://co-host/invite-landing?token=xxx) works with the existing scheme: "skye-hosts" config. For production, you'll want
  universal links (HTTPS) too so the link works when the app isn't installed.
  - Email sending is stubbed — the owner shares the invite link manually (Share sheet).
  - The sign-up-via-invite flow collects phone + OTP + name, but doesn't yet set the invitee's email on their account. You'll need a PATCH /account endpoint or add
  an email param to phoneVerifyOtp to link the invite email to the new account. Without this, the acceptInvite email check will fail for phone-only signups. Want me
  to add that?
- Favourites (listings)



===============
Pending Prompts
===============
- On guests websites on /listing/1/bluebell-croft-8 route.
- Put the entire page content inside an 1120 centered container (mui)
- Using attached images reference, copy the style for: Listing title, images & key info e.g 4 guests1 bedroom3 beds1 shared bath (ignore everything else)
- There are two layouts:
- First layout < md breakpoint
  - Use /example-below-md.jpg as reference.
  - Back button overlayed at top left corner of image
  - Heart icon overlayed at top right corner of image
  - Bottom right has {currentImage / totalImages} count
  - Swiping left or right cycles through images
  - Tapping: No current behaviour (Implemented later)
  - Unearth image:
    - Centered listing title:
    - Centered: descreiption, e.g 'Hut in Highland Perthshire, United Kingdom,'
    - Centered: 2nd descripotionL: '2 guests - 2 beds - 0 bathrooms' (Use bullet as sepearator)
- Second layout >= md breakpoint
  - 5 images 
  - use /example-md-and-above.png as reference
  - Contains 1 large image on left, with 4 on a grid to the right
  - Swiping: no behaviour ever to imlemented. In future a click will open aanother page.
  - Tapping: No current behaviour (Implemented later)
  - Below image, same content displayed as. < md breakpoint but its left aligned
- Parse docs/plans/listing-image-ratio-display-strategy.md to understand and apply css dynamic centering., Ensuring oyu use images large enough to avoid detrimental blur
- Any major decisions, ask, particularily on how to implement swipe if it requires external libraries

- Retain that 5 image layout above or equal to md breakpoint (But don't use exact value, use nearest breakpoint)
- Below that breakpoint it should only show 1 image (the cover image). (example-below-md.jpg)
- At this size, you can swipe left or right to cycle through the images
- At any breakpoint when a photo is tapped
- Endpoint should only show 'Listed' listings,  
- Update the display of listings, each item has following:
  - Parse docs/plans/listing-image-ratio-display-strategy.md to understand image display strategy using css centering
  - Pick which derived size is best used, avoid blur caused by css centering
  - 215 wide x 205 high (approx) image with 4px border radius (Make 4px sitewide standard where appropriate)
  - Use the listing cover photo for this image
  - Underneath:
    - {ListingTypeId} in 'suburb' (Put suburb as static placeholder for now)
    - £123/night (Static placeholder for now)
    - {LISTING_HIGHLIGHT_LABELS} value right aligned on same line as £123/night
  - Each row can have a max of 7 items
  - Each column should have smaller gutter (Use standard gutters we have or 1/2 it if you think optimal)
  - Each row should have 40-50 pixels gap 
- Any major decisions ask me

215 x 205 (approx)

Review unstaged files as see if there are any opportunities for improvement with regards to maintainability, DRY adherence and customer experience. If any
opportunities identified, present a plan to me for approval. Once granted, apply the changes as a minimal unified diff, without rewriting the entire file


==================
Re-useable prompts
==================
Review unstaged files as see if there are any opportunities for improvement with regards to maintainability, DRY adherence and code simplification. If any
opportunities identified, present a plan to me for approval. Once granted, apply the changes as a minimal unified diff, without rewriting the entire file


===========================
Notes about copying AirBNB
==========================
You cannot copy:
  Visual layouts that are highly distinctive
  Icons, illustrations, photos
  Exact wording
  Screen designs that are “substantially similar”
  Function is OK. Expression is protected.
You can't market as "AirBNB but cheaper"
TLDR:
  - Don't use exact wording
  - Diverge layout/style slightly where possible


