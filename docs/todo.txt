===
MVP
===
-ability to delete listing draft
- set Lat/Lng overide on create listling when env toggled
- Icons in App need some love, perhaps purple
- Decide & implement if public booking possible via signup during booking flow.
- eas build pipe failed
- Test graceful failure error displayed when uploading listing image in App
  - redeploy app
  - unset AWS_CLOUDFRONT_IMAGES_DOMAIN
  - test if image upload failure gracefully shows error
- Co - host test invite and listing access [Blocked awaiting dnb thing]
  - Resend: Deep link solve.
    - Publish app to google play (Beta)
    - docs/user-todos/universal-links-setup.md
      - ## Android (App Links)
      - ## App Store URLs
      - ## Native Rebuild Required
  - Get Expo pipeline (GH) deployed to real device
- Add 'Local laws' AirBnB equivlant to listing creator
- App: Edit listing
  - (Your space, remaining components)
    - Instant book
    - Cancellation policy
  - (Arrival guide remaining components)
  - Understanding why some data not colecting during creation, only edit... -> Plan
  - Draft or Publish after creation?
  - Availability
  - Pricing
- App: Calendar
- App: Co-host
  - Send Co-hist invite via twilio sms
- Booking events
  - Requested & confirmed
  - Instant
  - Cancellation
- Scheduled messages: Understanding, if should show in conversation thread
- Payments
- SKYE_ENVIRONMENT doesn't make much sense in context of app. as it has local, preview, production etc expo dists...
  - It's used by sentry, its passed it in as sentry env... also look at eas.json.

=======
General
=======
- flex-grow on <main not working>
- Shift stripe to skyehosts email & update secrets
- expo build working on real device?
- env plan: Pipeline green, deployed agian
  - ignore test-results
- Update pre-push to on qa branch only do aws-infra gate
- Update deploy-prod, to only deploy cdk if there's been a change
- add sentry dsn to expo vars for deployed app?

=============
Admin website
=============

==================
Aws infrastructure
==================
- Queue
  - Queue policy needs review (retry speed etc)
  - DLQ tested as working

========
Host App
========
- Listing title, vertical height is too long
- Test
  - Notification
- Login / Sign - up separation in app.?
- deploy to device succesfully via eas / gha
- Notifications (Messages from guest)
- Today page
- Calendar page
- If you already signed up and reinstalled app, could you gain access
- Security: Confirm peoples identity or address, otherwise anyone could hots a prop and collect money?
  - Decide address upload or stripe verification (account vs name match)

=============
Guest website
=============
- Check that sentry error 'def' capture dby sentry (isr)

===============
Long term TODOs
===============
- Test face auth (IOS) biometrics, as it uses face not fingerprint apparently.
- docs/user-todos/universal-links-setup.md
  - ## Apple (iOS Universal Links)
- Improve twilio email verification spam folder avoidance
  e.g configure SPF, DKIM, DMARC
- How can I test host app from my device if not dev API, does it matter?
- Enable tracing for sentry? - Can exceed free tier though...
- Stripe secret and publishable key on prod are using sandbox creds, need replaced
- Review const protectedPaths = ['/dashboard', '/bookings', '/account']; in each app
- Solve env vars in apps/f/e better as vercel doesn't support org level env vars
  and dont want repat across projects. but want to keep simple and no anti pattern.
- Next Steps for You (Cohost)
  - The invite link deep link (skye-hosts://co-host/invite-landing?token=xxx) works with the existing scheme: "skye-hosts" config. For production, you'll want
  universal links (HTTPS) too so the link works when the app isn't installed.
  - Email sending is stubbed — the owner shares the invite link manually (Share sheet).
  - The sign-up-via-invite flow collects phone + OTP + name, but doesn't yet set the invitee's email on their account. You'll need a PATCH /account endpoint or add
  an email param to phoneVerifyOtp to link the invite email to the new account. Without this, the acceptInvite email check will fail for phone-only signups. Want me
  to add that?

===============
Pending Prompts
===============
- In hosts app on Availability page
- Add a new list item with:
  - title: [You devicd]
  - Description: [You decide]
  - Tap behaviour:
    - Opens a modal
- Modal contains:
  - A react-native-paper switch component to set:
    - Dates unavailable by default or Dates available by default
    - Default selected option is 'available by default' (Which is set to true when listing created)
  - If available bny default is set to true, an additional field is displays:
  - Availability window
    - Description: How far in advance can guests book?
    - Options: 3 months, 6 months, 9 months, 12 months.
  - Re-use existing modals but only if appropriate
  - Values should be saved to listing.entity.ts
- Scrutinise my approach
- If more optimal approaches, recommend them
- Any non-trivial decisions, ask me
- Review any existing behaviour that this may affect with regards to Calendars, bookings, availability, ical Import/export etc and update accordingly.
- Note: Current state may include an implicit assumption that days are available unless blocked however, this assumption is now dependent on Dates available by default being set to true


No, I want to change the plan again

Key assumptions:
  - Practically all hosts will already be using AirBNB and they will have Import Calendar sync enabled
  - In most cases, hosts will have their AirBnB calendar set to 'unavailable by default' selected due to the seasonal nature of their BNB
  - AirBnB will normally be their master system that receives most bookings and it's ok for Skye Hosts to use that as master reference
Decisions:
  - No definition of open seasons via date picker
  - Do not give hosts the choice to select 'Unavailable by default' or 'Available by default' The system will always use 'Available by default' implicitly
  - Hosts will have the ability to block dates (or undo the block) on the Calendar listing page by dragging their thumb to select multiple days. This should open a slider from the bottom to make the toggle.
  - iCal export will always include Skye Hosts bookings
Choices:
  - iCal export may or may not include bSkye Hosts blocked dates
- Risks:
  - Host's calendar will always be open, on a rolling basis, given seasonal nature this may not be ideal but if AirBnB calendar is set to 'unavailable by default' and iCal import enabled, this should prevent issues whereby rolling future dates are inadvertently opened
  - If iCal export includes Skye Hosts blocked dates, and if iCal export URL added to Air BNB at a latter time it may have unintended conseuqnces of blocking dates off in AirBnB calenar. 
  - Clarify for hosts. We need to increase clarity nad present ghints in laymans terms for hosts who are non-technical to ensure that they understand how the interplay between Skye Hosts and AirBnb works
    - Most importantly:
      - Dates are opened by default
      - We recommend their AirBnB is set to 'unavailable by default' to preveent rolling open dates
      - iCal export mchoice
      - etc
- Goals:
  - Keep complexity low
  - Keep mental load low
  - Opinionated, easy to understand system
  - Ensure proper guidance provided to hosts
- Scope of implementation
  - Ability to block or undo a block on one or many dates on the listing calendar page in hosts app.
  - Selecting multiple cells can be done via a thumb drag
  - Scan for any existing implementations, particuarily around iCal import/export, booking, listing date pickers on guest website that may require changes due to this plan
  - Add doc to docs/architecture explaining how this work.
  - Any work you consider beneficial beyond this initial delivery, identify it so i can save it for later.
- Scrutinise my plan, challenge assumptions, decisions from the context of complexity, user experience of a Highlands host, Maintenance.
- Any opportunities for imporvement, recommend them
- Any big decisions, ask

==================
Re-useable prompts
==================
Review unstaged files as see if there are any opportunities for improvement with regards to maintainability, DRY adherence and code simplification. If any
opportunities identified, present a plan to me for approval. Once granted, apply the changes as a minimal unified diff, without rewriting the entire file

==================
Things to test
==================
- iCal export
- iCal booking.com

==========================
Notes about copying AirBNB
==========================
You cannot copy:
  Visual layouts that are highly distinctive
  Icons, illustrations, photos
  Exact wording
  Screen designs that are “substantially similar”
  Function is OK. Expression is protected.
You can't market as "AirBNB but cheaper"
TLDR:
  - Don't use exact wording
  - Diverge layout/style slightly where possible

On both thes pages in host app:
  - Create message template
  - Edit message template.
