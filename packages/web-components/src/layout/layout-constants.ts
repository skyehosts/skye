/** Shared by header, footer, and page container so content edges align. */
export const CONTENT_MAX_WIDTH = 1800;

/** Shared by header, footer, and page container so content edges align. */
export const contentPaddingX = { xs: 2, sm: 3 } as const;
